import { describe, it, expect } from 'vitest';

// Milestone 1 placeholder: proves the test runner is wired up.
// Real engine tests (determinism, architecture, long-run) arrive in M2+.
describe('project skeleton', () => {
	it('runs the test suite', () => {
		expect(1 + 1).toBe(2);
	});
});
