import type { SimContext } from '../context';
import type { World } from '../models/world';

/**
 * Phase 10 — Strategic decisions (BLUEPRINT.md §20–§21, §24; MODEL.md §51–§53):
 * each state scores its available actions and picks probabilistically. No-op
 * scaffold; milestone 15.
 */
export function makeStrategicDecisions(_world: World, _ctx: SimContext): void {}
