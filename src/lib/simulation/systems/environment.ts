import type { SimContext } from '../context';
import type { World } from '../models/world';

/**
 * Phase 1 — Environment (BLUEPRINT.md §24). Harvest variation and other small
 * seeded shocks (MODEL.md §67). No-op scaffold; filled in a later milestone.
 */
export function updateEnvironment(_world: World, _ctx: SimContext): void {}
