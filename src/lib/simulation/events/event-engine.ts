import type { SimContext } from '../context';
import type { World } from '../models/world';

/**
 * Phase 13 — Events (BLUEPRINT.md §26, §24; MODEL.md §65–§66): derive
 * `WorldEvent`s from this year's state changes, each with importance, actors,
 * and causes. No-op scaffold; milestone 18.
 */
export function generateEvents(_world: World, _ctx: SimContext): void {}
